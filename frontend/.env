# Development environment variables
# This file configures the frontend to use local backend during development

# Leave empty to use Vite proxy (recommended for local development)
# The proxy is configured in vite.config.ts to forward /api requests to localhost:8000
VITE_API_URL=

# Alternative: Set to full URL if you want to bypass the proxy
# VITE_API_URL=http://localhost:8000

# For production deployment, set this to your production API URL
# VITE_API_URL=http://107.20.129.68
