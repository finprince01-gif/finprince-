# Leave VITE_API_URL empty to use Vite's built-in proxy (configured in vite.config.ts)
# For local dev, Vite proxies /api to http://localhost:8000
VITE_API_URL=

# Landing Page URL
VITE_LANDING_URL=http://localhost:3000

# Server Port
VITE_PORT=5173
