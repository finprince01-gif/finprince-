Need a full infrastructure audit for Zoho Bulk Scan upload flow.

DO NOT change any existing logic yet.

Need to VERIFY whether Zoho Bulk Scan is actually using the hardened distributed pipeline architecture that was built earlier.

Audit the COMPLETE execution path from frontend upload → finalization.

Need verification for ALL of these:

1. SQS PIPELINE
Verify Zoho upload uses:
- ingestion queue
- ai queue
- assembly queue
- finalize queue
- export queue

Need exact enqueue + dequeue trace.

2. DISTRIBUTED WORKERS
Verify processing is happening through:
- ingestion workers
- AI workers
- assembly workers
- finalize workers

Need proof from logs and execution path.

3. REDIS USAGE
Verify Zoho flow uses:
- Redis locks
- Redis progress state
- Redis coordination
- visibility extension state
- barrier/finalization sync

Need exact Redis keys and code paths.

4. CLUSTER EXECUTION
Verify:
- ThreadPoolExecutor / worker pools
- parallel page execution
- distributed assembly
- snapshot aggregation
- async orchestration

Need exact code locations.

5. FRONTEND POLLING
Verify frontend polling is connected to distributed backend state and not local component-only state.

Trace:
- bulk-status polling
- hydration polling
- finalize detection
- UI transition state

6. STORAGE FLOW
Verify whether Zoho uses:
- snapshot offload
- canonical payload persistence
- staging payloads
- export payloads

Need exact storage path.

7. FAILURE ISOLATION
Verify:
- one invoice failure does not kill batch
- retries work
- partial_failed status handling works
- visibility timeout extension works

8. FINALIZE FLOW
Verify FINALIZED state is generated by distributed finalize worker and not local UI inference.

9. SCALING VALIDATION
Need confirmation whether Zoho flow is:
- horizontally scalable
- multi-worker safe
- concurrency safe
- idempotent
- safe for 1000+ uploads

10. MOST IMPORTANT:
Need explicit comparison:
- Purchase Scan flow
vs
- Zoho Bulk Scan flow

Need to identify:
- what infrastructure components Zoho is bypassing
- what components are still legacy
- what components are fully distributed

IMPORTANT:
DO NOT modify code yet.

Need only:
- root cause audit
- execution trace
- architecture verification
- missing distributed integrations
- bottlenecks
- legacy bypasses
- unsafe synchronous paths
- UI/backend mismatches

Need final report with:
- VERIFIED WORKING
- PARTIALLY CONNECTED
- BYPASSED
- LEGACY PATH
- HIGH RISK
sections.